        log_format  file '$time_local || $remote_addr || $upstream_addr || $status || $request_time || $upstream_response_time || $connection_requests || $body_bytes_sent || $host || $request_method || $http_user_agent || $document_uri || $request_uri || $http_referer || $http_cookie';
        log_format  main '$time_local || $http_x_forwarded_for || $upstream_addr || $status || $request_time || $upstream_response_time || $connection_requests || $body_bytes_sent || $host || $request_method || $http_user_agent || $document_uri || $request_uri || $http_referer || $http_cookie'
